# SmartSalai/NLP Forensic Audit — Remediation Plan

**Date:** April 6, 2026  
**Phase:** 1 (Implementation)  
**Status:** IN PROGRESS

---

## Priority Tiers & Implementation Sequence

### TIER 1: CRITICAL (Security Vulnerabilities)
Must fix before deployment. SQL injection and auth bypass risks.

| # | Issue | File | Line(s) | Severity | Fix Type |
|---|-------|------|---------|----------|----------|
| 1 | SQL F-String Construction | `agents/blackspot_mapper.py` | 263–278 | **CRITICAL** | Replace f-string with parameterized INSERT |
| 2 | Truncated BLE Auth (4-byte HMAC) | `agents/ble_mesh_broker.py` | 68 | **CRITICAL** | Use full 32-byte HMAC-SHA256 |
| 3 | Insecure Auth Defaults | `agent2_dashboard/api.py` | 32 | **CRITICAL** | Load from environment; no hardcoded fallback |
| 4 | Insecure Auth Defaults | `core/firebase_client.py` | 13 | **CRITICAL** | Load from environment; raise if missing |
| 5 | Insecure Auth Defaults | `core/knowledge_ledger.py` | 19 | **CRITICAL** | Load from environment; raise if missing |

**Acceptance Criteria:**
- [ ] No f-string SQL; all use parameterized `?` placeholders
- [ ] HMAC signatures are 32 bytes; tests validate cryptographic integrity
- [ ] All secret keys loaded from environment variables; no hardcoded fallbacks
- [ ] Unit tests pass for SQL/BLE/auth modules

---

### TIER 2: HIGH (Functional Crashes & Security)
Prevent runtime crashes and data leakage.

| # | Issue | File | Line(s) | Severity | Fix Type |
|---|-------|------|---------|----------|----------|
| 6 | Unvalidated GPS Assignment | `scripts/live_road_test.py` | 83–84 | **HIGH** | Add type checks, range validation (-90 to 90 lat, -180 to 180 lon) |
| 7 | Unguarded Inference Path | `scripts/live_road_test.py` | 168–172 | **HIGH** | Wrap in try/except; log + fallback on error |
| 8 | Video Stream Token Leakage | `dashboard_api.py` | 763 | **HIGH** | Move token from query param to Authorization header |
| 9 | Silent Sensor Polling Failures | `scripts/live_road_test.py` | 72, 85 | **HIGH** | Log exceptions; emit degradation events instead of silently passing |
| 10 | SOS Cancel Name Error | `agents/sos_responder.py` | 145 | **HIGH** | Move `incident_id` definition into correct scope (before cancel handler) |
| 11 | UI Formatting Crash | `scripts/live_road_test.py` | 183 | **HIGH** | Validate GPS before formatting; use default if None |

**Acceptance Criteria:**
- [ ] GPS validated at assignment; invalid values logged with incident ID
- [ ] Inference failure logged and caught; UI displays "INFERENCE_UNAVAILABLE" gracefully
- [ ] Token moved to Authorization header; query param removed from endpoint
- [ ] Sensor failures emit "SENSOR_DEGRADATION" events on bus; graceful fallback active
- [ ] SOS cancellation references correct `incident_id` in scope
- [ ] Live test harness displays "GPS: [UNAVAILABLE]" if coords invalid

---

### TIER 3: MEDIUM (Reliability & Memory)
Prevent resource exhaustion and improve stability.

| # | Issue | File | Line(s) | Severity | Fix Type |
|---|-------|------|---------|----------|----------|
| 12 | Unbounded Mesh Cache | `agents/ble_mesh_broker.py` | 47, 100 | **MEDIUM** | Replace `set` with `collections.deque(maxlen=1000)` or LRU cache |
| 13 | Unbounded Telemetry Queue | `core/knowledge_ledger.py` | 35 | **MEDIUM** | Add `maxsize=5000` to `queue.Queue()` |

**Acceptance Criteria:**
- [ ] `seen_messages` is bounded; oldest messages evicted when limit reached
- [ ] `_write_queue` has max 5000 items; producers block if queue full (backpressure)
- [ ] Memory profiling shows no growth under 100K events/minute

---

### TIER 4: LOW (Code Quality & Mock Cleanup)
Remove test/mock code for production readiness.

| # | Issue | File | Line(s) | Severity | Fix Type |
|---|-------|------|---------|----------|----------|
| 14 | Mock Research Responses | `core/research_clients.py` | 142–171 | **LOW** | Remove + unit test → real API or raise ConfigError |
| 15 | Legal Document Mocks | `etl/legal_document_processor.py` | 326–332 | **LOW** | Remove smoke test mocks; use pytest fixtures instead |
| 16 | Dummy Legal PDF Creation | `agents/legal_syncer.py` | 75–79 | **LOW** | Remove; require valid source or skip sync |
| 17 | Mock IMU Bus Emission | `agents/sentinel_fusion.py` | 92–98 | **LOW** | Remove; emit only on actual sensor data |
| 18 | Hardcoded Accident Data | `agents/blackspot_mapper.py` | 331–332 | **LOW** | Remove; load from database or skip initialization |

**Acceptance Criteria:**
- [ ] No mock/fake data in production code paths
- [ ] All test code isolated to `tests/` directory
- [ ] CI/CD blocks if mock patterns detected via linting rules

---

## Implementation Notes

### Parallelizable Fixes (Can apply simultaneously)
- Fix 1, 2, 3: SQL/BLE/Auth (independent modules)
- Fix 6, 7, 9, 11: live_road_test.py (same file, non-overlapping lines)
- Fix 14–18: Mock cleanup (independent removals)

### Dependencies
- **Fix 5 → Fix 3:** knowledge_ledger imports firebase_client; must fix auth first
- **Fix 8 → Upstream:** dashboard_api.py must be reviewed for other query-param auth uses
- **Fix 10:** Minimal dependency; can be applied independently

### Testing Strategy
**Unit Tests Required:**
- `test_sql_parameterization()`: Verify blackspot_mapper uses `?` placeholders
- `test_ble_hmac_full_length()`: Verify HMAC is 32 bytes
- `test_auth_from_env()`: Verify no hardcoded defaults
- `test_gps_validation()`: Verify invalid coords rejected
- `test_sensor_failure_logged()`: Verify exceptions not silently swallowed
- `test_unbounded_cache_bounded()`: Verify cache/queue have max sizes

**Integration Tests Required:**
- End-to-end live_road_test.py with GPS unavailable (should gracefully degrade)
- SOS trigger + cancel flow (incident_id scope test)
- Video stream authorization (Authorization header required, query param blocked)

---

## Status Tracking

**Tier 1 (CRITICAL):** ⬜ Not Started
**Tier 2 (HIGH):** ⬜ Not Started
**Tier 3 (MEDIUM):** ⬜ Not Started
**Tier 4 (LOW):** ⬜ Not Started

---

## Rollback Plan

Each fix includes reversible change:
1. Git branch per fix: `fix/sql-injection`, `fix/ble-auth`, etc.
2. Full test suite runs before merge to `main`
3. Revert command: `git revert <commit_hash>` per fix
4. Emergency rollback: Switch to pre-audit tag

